services:
  - type: web
    name: aegis-dashboard
    plan: free
    runtime: docker
    dockerfilePath: ./Dockerfile.streamlit
    dockerContext: .
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: AEGIS_ALLOW_BUILTIN_YOLO
        value: "false"
